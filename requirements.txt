#
# Base dependencies pinned via pip freeze (Python 3.13)
#
asgiref==3.11.0
dj-database-url==3.0.1
Django==5.2.17
django-environ==0.12.0
gunicorn==23.0.0
Pillow==12.0.0
whitenoise==6.6.0
psycopg[binary]==3.3.2
sqlparse==0.5.5
tzdata==2025.3

# Painel Macro / scraping / tasks
requests==2.32.5
beautifulsoup4==4.14.3
pandas==2.3.3
numpy==2.2.6
openpyxl==3.1.5
playwright==1.49.0
celery==5.3.6
redis==5.0.8

# Análise por IA (OpenAI GPT)
openai==1.55.3

# Rate limiting (login, registro)
django-ratelimit==4.1.0

# Observabilidade. Sem `SENTRY_DSN` no ambiente, o SDK nem é inicializado
# (ver prod.py): fica no disco sem custo de runtime até alguém colar o DSN.
sentry-sdk==2.68.1
